# دستورالعمل‌های ایجنت (Agent Instructions)

- **عدم ایجاد لینک برای موارد عمومی و غیرواقعی:** جاهایی که واقعاً لینک نیستند یا فایل مشخصی در ورک‌اسپیس نیستند (مانند فایل‌فرمت‌ها یا الگوهای عمومی مثل `design.md` یا `skill.md` یا `agent.md` که صرفاً به عنوان مثال یا فرمت بیان می‌شوند)، به صورت لینک رندر نشوند. برای نمایش آن‌ها از کدبلاک اینلاین (مثل \`design.md\`) استفاده شود.
- **ایجاد لینک فقط برای فایل‌های واقعی:** فقط برای فایل‌هایی که واقعاً در ورک‌اسپیس وجود دارند و نیاز به ارجاع دقیق است، لینک کلیک‌پذیر (با ساختار `file:///...`) ساخته شود.
